{
  "version": 2,
  "installCommand": "npm install && (cd server && npm install)",
  "buildCommand": "npm run build && cd server && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
